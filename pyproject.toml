[tool.pytest.ini_options]
addopts = "--cov=src --cov-report=xml --cov-fail-under=70"

[tool.ruff]
line-length = 127
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "S", "UP", "I"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]
